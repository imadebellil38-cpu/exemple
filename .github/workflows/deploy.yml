name: Deploy

on:
  push:
    branches: [main, master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: []  # Add 'test' job dependency when CI is set up
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js 20.x
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: 'npm'

      - name: Install production dependencies
        run: npm ci --production

      # Placeholder for actual deployment
      # Uncomment and configure one of these based on your hosting:

      # Option 1: Deploy to Railway
      # - name: Deploy to Railway
      #   uses: bervProject/railway-deploy@main
      #   with:
      #     railway_token: ${{ secrets.RAILWAY_TOKEN }}

      # Option 2: Deploy to Render
      # - name: Deploy to Render
      #   run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}

      # Option 3: Deploy to VPS via SSH
      # - name: Deploy via SSH
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.SSH_HOST }}
      #     username: ${{ secrets.SSH_USER }}
      #     key: ${{ secrets.SSH_KEY }}
      #     script: |
      #       cd /var/www/prospecthunter
      #       git pull
      #       npm ci --production
      #       pm2 restart prospecthunter

      - name: Build summary
        run: |
          echo "## Deploy Ready" >> $GITHUB_STEP_SUMMARY
          echo "Deployment placeholder - configure your hosting provider secrets" >> $GITHUB_STEP_SUMMARY
